All changes are done. Here's a full summary of everything implemented:

What was built
Domain model
campaign_stats_get.dart — Added submitted, approved, revisionRequired fields to CampaignStat (all default to 0 for backwards compat)

batch_process_applications.dart — Added RequestRevisionRequest / RequestRevisionResponse

API layer
endpoints.dart — Added campaignRequestRevision = 'campaigns/request-revision'

campaign_repository_interface.dart — Added requestRevision() abstract method

brand_campaign_dio_repository.dart — Implemented requestRevision()

Brand UI
campaign_stats_view.dart — New "Content Review" section with 3 coloured cards (blue = Submitted, green = Approved, orange = Needs Revision) + added to PDF report

influencer_detail_bottom_sheet.dart — Added actionStatus param + orange "Revision" pill button when actionStatus == 'completed' on an active campaign + _showRevisionDialog with reason text input

campaign_influencers_view.dart — Passes actionStatus through to the bottom sheet

Creator UI
campaign_details_view.dart — Three updates:

Orange "Re-submit" button label when revision_required
Orange background colour for the action button
Amber banner above the tasks section explaining what needs to be done


implemented a cron job for reliability score service