Clean compile. Here's a summary of what was done:

Changes made:

campaigns.model.ts

campaignStats() — Added i.application_status to the SQL query, added counting for submitted, approved, revisionRequired, and included them in the returned stats object
activityComplete() — Updated to accept action_status IN ('started', 'revision_required') so creators can resubmit after revision
requestRevision() — New method: validates the creator has a completed submission, sets action_status = 'revision_required', fires a REVISION_REQUIRED notification
campaigns.ts

POST /request-revision route (brand-protected)
requestRevision controller function
Full status flow is now tracked:

Status	How it gets set
invited	Brand sends invite
accepted	Creator accepts
submitted	Creator submits application
approved	Brand approves application
started	Creator starts work
completed	Creator marks tasks done
revision_required	Brand calls POST /request-revision
rejected	Brand calls POST /reject-submission
